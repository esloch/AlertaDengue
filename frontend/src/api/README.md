# API Clients

Add typed frontend clients here as Django/DRF endpoints are introduced for the SPA migration. Start with stable contracts for high-use data surfaces such as notifications, alert history, and home chart data.
